<!DOCTYPE html>
<html>
  <head>
    <title>TwitterX</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-black">
    <% unless request.path == new_user_session_path || request.path == new_user_registration_path %>

      <nav class="w-16 md:w-64 min-h-screen bg-black p-6 fixed flex justify-center border-r border-gray-700 transition-all duration-300">
        <ul class="space-y-8">
          <li>
            <%= link_to '#', class: "mt-14 flex items-center space-x-2 text-white hover:text-gray-300 text-xl hover:bg-gray-900 hover: py-2 px-4 hover:rounded-2xl " do %>
              <span class="mr-5 ml-5 sm:ml-4 md:ml-0"><%= render "shared/icons/home" %></span>
              <span class="hidden md:inline">Home</span> <!-- Text hidden on small screens -->
            <% end %>
          </li>
          <li>
            <%= link_to '#', class: "flex items-center space-x-2 text-white hover:text-gray-300 text-xl hover:bg-gray-900 hover: py-2 px-4 hover:rounded-2xl" do %>
              <span class="mr-5 ml-5 sm:ml-4 md:ml-0"><%= render "shared/icons/profile" %></span>
              <span class="hidden md:inline">Profile</span>
            <% end %>
          </li>
          <li>
            <%= link_to '#', class: "flex items-center space-x-2 text-white hover:text-gray-300 text-xl hover:bg-gray-900 hover: py-2 px-4 hover:rounded-2xl" do %>
              <span class="mr-5 ml-5 sm:ml-4 md:ml-0"><%= render "shared/icons/settings" %></span>
              <span class="hidden md:inline">Settings</span>
            <% end %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, data: { "turbo-method": :delete }, class: "flex items-center space-x-2 text-white hover:text-gray-300 text-xl hover:bg-gray-900 hover: py-2 px-4 hover:rounded-2xl" do %>
              <span class="mr-5 ml-5 sm:ml-4 md:ml-0"><%= render "shared/icons/logout" %></span>
              <span class="hidden md:inline">Logout</span>
            <% end %>
          </li>
        </ul>
      </nav>
    <% end %>

    <main class="mt-4 px-5 bg-black transition-all duration-300 ml-16 md:ml-64">
      <%= yield %>
    </main>
  </body>
</html>
